import { styled } from "stitches.config";

/* eslint sort-keys: 0 */

const BlockStyled = styled("div", {
  marginBottom: "$gr6",

  "@md": {
    marginBottom: "$gr5",
  },
});

export { BlockStyled };
