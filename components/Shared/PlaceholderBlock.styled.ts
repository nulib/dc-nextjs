import { styled } from "@/stitches.config";

const PlaceholderBlock = styled("section", {
  background: "$black10",
  margin: "0",
  padding: "$2 0",
  textAlign: "center",
});

export { PlaceholderBlock };
