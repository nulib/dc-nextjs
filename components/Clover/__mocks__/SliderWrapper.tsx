import React from "react";

const SliderWrapperMock = ({ collectionId }: { collectionId: URL }) => (
  <div>Mock SliderWrapper with props: {collectionId.toString()}</div>
);

export default SliderWrapperMock;
