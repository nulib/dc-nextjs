import { styled } from "@/stitches.config";

/* eslint sort-keys: 0 */

export const FooterStyled = styled("div", {
  background: "$purple",
  padding: "$gr2 0",

  "a, a:visited": {
    color: "$white !important",
  },
});
